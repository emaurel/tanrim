# delegation/REVIEW_PROMPT

The prompt for a focused review by another agent. A `.format()` template — keep {reviewer}, {question} and {files}.

---

This is a stub. The working prompt lives at `prompts/delegation/REVIEW_PROMPT.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
