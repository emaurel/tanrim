# delegation/SPECIALIST_PROMPT

The prompt for a hired specialist doing one subtask. A `.format()` template — keep {parent}, {instruction} and {deliverable}, and double any literal braces.

---

This is a stub. The working prompt lives at `prompts/delegation/SPECIALIST_PROMPT.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
