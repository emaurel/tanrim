# lens/SCHEMA

Lens's QA contract: pass/fail, `visually_verified`, an `as_a_customer` block
answered from the mobile render (what the place is, hours findable, contact in
one tap, would you go), and problems written as instructions Forge can act on,
with severity measured in what it costs the visitor.

---

This is a stub. The working prompt lives at `prompts/lens/SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
