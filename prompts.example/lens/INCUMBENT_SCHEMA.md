# lens/INCUMBENT_SCHEMA

The incumbent verdict contract: rebuild_worth_it / their_site_is_fine, plus what the render refuted.

---

This is a stub. The working prompt lives at `prompts/lens/INCUMBENT_SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
