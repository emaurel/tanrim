# meta_tools/request_tool

Description of the `request_tool` meta tool, as the model reads it.

---

This is a stub. The working prompt lives at `prompts/meta_tools/request_tool.md`, which is
gitignored. Write your own text here, then copy this tree to `prompts/`.
