# meta_tools/delegate_subtask

Description of `delegate_subtask` — when an agent should hire a specialist and when it shouldn't.

---

This is a stub. The working prompt lives at `prompts/meta_tools/delegate_subtask.md`, which is
gitignored. Write your own text here, then copy this tree to `prompts/`.
