# nova/SCHEMA

Nova's output contract: a list of leads, or a `deferred` object when it only escalated.

---

This is a stub. The working prompt lives at `prompts/nova/SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
