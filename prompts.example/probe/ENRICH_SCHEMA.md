# probe/ENRICH_SCHEMA

The dossier contract, including `build_readiness` and `content_gaps`.

---

This is a stub. The working prompt lives at `prompts/probe/ENRICH_SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
