# probe/ROLE

Probe's qualification pass: is the business real, trading, reachable, and genuinely web-deficient. Must hand off rather than judge a site it only fetched.

---

This is a stub. The working prompt lives at `prompts/probe/ROLE.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
