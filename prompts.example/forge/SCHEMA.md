# forge/SCHEMA

Forge's build summary contract: sections, palette, facts used, placeholders, design rationale.

---

This is a stub. The working prompt lives at `prompts/forge/SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
