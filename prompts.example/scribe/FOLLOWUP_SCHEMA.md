# scribe/FOLLOWUP_SCHEMA

The JSON contract for a follow-up note: language, subject, body, the single
thing it asks for, and a self-check field asserting the note does not repeat
the pitch.

---

This is a stub. The working prompt lives at `prompts/scribe/FOLLOWUP_SCHEMA.md`,
which is gitignored. Write your own text here, then copy this tree to `prompts/`.
