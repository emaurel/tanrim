# scribe/COPY_SCHEMA

The site-copy contract: title tag, meta description, headings, sections.

---

This is a stub. The working prompt lives at `prompts/scribe/COPY_SCHEMA.md`, which is
gitignored — the prompts are the part of this project worth keeping
private. Write your own text here, then copy this tree to `prompts/`.
