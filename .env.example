# Required
ANTHROPIC_API_KEY=

# ---- Agency identity (required before ANY outreach can be sent) ----
# Cold B2B outreach needs a real, identifiable sender and a working opt-out.
# Echo refuses to raise a send-approval card until these two are set.
AGENT_ENV_AGENCY_NAME=
AGENT_ENV_SENDER_EMAIL=
AGENT_ENV_AGENCY_ADDRESS=

# What Scribe quotes for a spec site, in whole currency units.
AGENT_ENV_QUOTE_AMOUNT=450
AGENT_ENV_QUOTE_CURRENCY=EUR

# ---- Sending (optional) ----
# Without these, approving a send hands you the email to send yourself rather
# than sending anything. Start that way; turn it on once the copy is calibrated.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# ---- Hosting ----
# Where published previews are reachable. Local by default; point this at a
# real host (plus a matching deploy step in Courier) when you outgrow it.
AGENT_ENV_PREVIEW_BASE=http://127.0.0.1:8765/preview

# Server
AGENT_ENV_HOST=127.0.0.1
AGENT_ENV_PORT=8765
